# Visit Map Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** TOPページの費用ダッシュボード下に react-leaflet を使った訪問都市マップを追加する。

**Architecture:** `lib/visits.ts` に訪問都市データを定義し、`components/VisitMap.tsx`（`'use client'`）で Leaflet マップをレンダリング。`app/page.tsx`（Server Component）からは `next/dynamic` + `ssr: false` でインポートして SSR クラッシュを回避する。

**Tech Stack:** Next.js 16 App Router, React 19, TypeScript, react-leaflet ^4, leaflet ^1.9, Tailwind CSS

---

## File Map

| Action | Path | Responsibility |
|--------|------|----------------|
| Create | `lib/visits.ts` | 訪問都市の型定義とモックデータ |
| Create | `components/VisitMap.tsx` | `'use client'` — Leaflet マップ UI |
| Modify | `app/page.tsx` | dynamic import + セクション追加 |
| Modify | `package.json` / `package-lock.json` | leaflet, react-leaflet 追加 |

---

### Task 1: 依存パッケージのインストール

**Files:**
- Modify: `package.json`

- [ ] **Step 1: パッケージをインストール**

```bash
npm install leaflet react-leaflet
npm install --save-dev @types/leaflet
```

- [ ] **Step 2: インストール確認**

```bash
cat package.json | grep -E "leaflet|react-leaflet"
```

Expected output（実際のバージョン番号は環境による。`leaflet`、`react-leaflet`、`@types/leaflet` の3行が表示されればOK）:
```
"leaflet": "^1.9.4",       ← バージョン番号は実際の値
"react-leaflet": "^4.2.1", ← バージョン番号は実際の値
"@types/leaflet": "^1.9.4" ← バージョン番号は実際の値
```

- [ ] **Step 3: コミット**

```bash
git add package.json package-lock.json
git commit -m "chore: add leaflet and react-leaflet"
```

---

### Task 2: 訪問都市データ（`lib/visits.ts`）

**Files:**
- Create: `lib/visits.ts`

- [ ] **Step 1: `lib/visits.ts` を作成**

```ts
export type Visit = {
  id: string
  city: string
  lat: number
  lng: number
  date: string // YYYY-MM-DD
}

export const visits: Visit[] = [
  { id: '1', city: 'リスボン',     lat: 38.7223, lng:  -9.1393, date: '2026-01-05' },
  { id: '2', city: 'ポルト',       lat: 41.1579, lng:  -8.6291, date: '2026-01-10' },
  { id: '3', city: 'パリ',         lat: 48.8566, lng:   2.3522, date: '2026-01-18' },
  { id: '4', city: 'ダブリン',     lat: 53.3498, lng:  -6.2603, date: '2026-01-28' },
  { id: '5', city: 'ローマ',       lat: 41.9028, lng:  12.4964, date: '2026-02-05' },
  { id: '6', city: 'フィレンツェ', lat: 43.7696, lng:  11.2558, date: '2026-02-12' },
  { id: '7', city: 'ヴェネツィア', lat: 45.4408, lng:  12.3155, date: '2026-02-18' },
]
```

- [ ] **Step 2: TypeScript 型チェック**

```bash
npx tsc --noEmit
```

Expected: エラーなし（または visit-map 以外の既存エラーのみ）

- [ ] **Step 3: コミット**

```bash
git add lib/visits.ts
git commit -m "feat: add visit city mock data"
```

---

### Task 3: VisitMap コンポーネント（`components/VisitMap.tsx`）

**Files:**
- Create: `components/VisitMap.tsx`

- [ ] **Step 1: `components/VisitMap.tsx` を作成**

```tsx
'use client'

import L from 'leaflet'
import 'leaflet/dist/leaflet.css'
import { MapContainer, Marker, Popup, TileLayer } from 'react-leaflet'
import { visits } from '../lib/visits'

const markerIcon = L.divIcon({
  className: '',
  html: '<div style="width:14px;height:14px;border-radius:50%;background:#4a7c59;border:2px solid white;box-shadow:0 1px 4px rgba(0,0,0,.3)"></div>',
  iconSize: [14, 14],
  iconAnchor: [7, 7],
  popupAnchor: [0, -10],
})

export default function VisitMap() {
  return (
    <MapContainer
      center={[47, 10]}
      zoom={4}
      style={{ height: '400px', width: '100%' }}
      scrollWheelZoom={false}
    >
      <TileLayer
        attribution='&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a>'
        url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"
      />
      {visits.map((v) => (
        <Marker key={v.id} position={[v.lat, v.lng]} icon={markerIcon}>
          <Popup>
            <p style={{ fontWeight: 600, color: '#4a7c59', margin: '0 0 2px' }}>{v.city}</p>
            <p style={{ fontSize: '0.75rem', color: '#6b7280', margin: 0 }}>{v.date}</p>
          </Popup>
        </Marker>
      ))}
    </MapContainer>
  )
}
```

**ポイント:**
- `L.divIcon` を使うことで leaflet デフォルトアイコンの webpack パス問題を回避
- Popup 内はインラインスタイル使用（Leaflet の DOM オーバーレイで Tailwind クラスが当たらない可能性があるため）
- `popupAnchor: [0, -10]` でマーカー上にポップアップを表示

- [ ] **Step 2: 型チェック**

```bash
npx tsc --noEmit
```

Expected: エラーなし

- [ ] **Step 3: コミット**

```bash
git add components/VisitMap.tsx
git commit -m "feat: add VisitMap component with react-leaflet"
```

---

### Task 4: `app/page.tsx` にマップセクションを追加

**Files:**
- Modify: `app/page.tsx`

- [ ] **Step 1: `app/page.tsx` を編集**

ファイル先頭の import 行に `dynamic` を追加し、`<ExpenseCharts />` の後にセクションを挿入する。

**変更内容は2箇所。**

**① 1行目の直前に1行追加（`import dynamic` のみ）:**

既存の1行目:
```tsx
import { BUDGET, getTotalSpent, getBudgetRemaining, getByCategory, getByDate } from '../lib/expenses'
```

この行の前に以下を1行追加する（`import ExpenseCharts` は既存のため追加不要）:
```tsx
import dynamic from 'next/dynamic'
```

**② `import ExpenseCharts` の行の直後、`export default` の前にモジュールスコープで宣言を追加:**

既存の2行目:
```tsx
import ExpenseCharts from '../components/ExpenseCharts'
```

この行の直後に追加（関数の外、モジュールスコープ）:
```tsx
const VisitMap = dynamic(() => import('../components/VisitMap'), { ssr: false })
```

**③ `<ExpenseCharts byCategory={byCategory} byDate={byDate} />` の直後、`</main>` の前にセクションを追加:**
```tsx
      <ExpenseCharts byCategory={byCategory} byDate={byDate} />

      {/* Visit map */}
      <section className="mt-10">
        <h2 className="text-sm font-semibold mb-4" style={{ color: '#4a7c59' }}>
          訪問した場所
        </h2>
        <div className="rounded-xl overflow-hidden border border-gray-100 shadow-sm">
          <VisitMap />
        </div>
      </section>
    </main>
```

- [ ] **Step 2: ビルド確認**

```bash
npm run build
```

Expected: `✓ Compiled successfully` （エラーなし）

もし `Module not found: leaflet` 等のエラーが出た場合は Task 1 のインストールを再確認。

- [ ] **Step 3: 開発サーバーで目視確認**

```bash
npm run dev
```

ブラウザで `http://localhost:3000` を開き:
- 「訪問した場所」セクションが費用ダッシュボード下に表示される
- ヨーロッパ地図が高さ 400px で表示される
- 7都市にセージグリーンのマーカーが表示される
- マーカーをクリックすると都市名と訪問日のポップアップが表示される

- [ ] **Step 4: コミット**

```bash
git add app/page.tsx
git commit -m "feat: add visit map section to top page"
```

---

### Task 5: Vercel へデプロイ

**Files:**
- なし（push のみ）

- [ ] **Step 1: 最終ビルド確認**

```bash
npm run build
```

Expected: エラーなし、警告のみ許容

- [ ] **Step 2: origin/main へプッシュ**

```bash
git push origin main
```

Expected: Vercel が自動デプロイを開始する
